# 323.15 K口径统一说明

7200 s后的烘房恒温设定统一为 **50.00 ℃（323.15 K）**。温度场以摄氏度推进；只有附录3、附录4的Arrhenius扩散项在计算前换算为开尔文。

采用本口径重新计算后，问题三烘干时间为 **57.5471 h**，问题四为 **51.1197 h**。重新生成结果与核验的命令为：

```bash
python scripts/build_deliverables.py
python scripts/run_q4_audit.py
```
