# 问题四端面近似说明

题目未区分圆柱侧面和端面的换热系数。问题四以中截面径向场为目标，端面采用绝热近似，不额外引入没有数据支撑的端面参数。二维核验显示：在两端面施加相同条件时，两端面数值温差为 \(4×10^{-13}\) K；瞬态轴向温差上限为 \(0.105\) K，对烘干时长的影响不超过 \(0.39\%\)，且随时间衰减。因此绝热端面是该中截面一维模型的保守近似，并不表示真实端面不存在换热。
