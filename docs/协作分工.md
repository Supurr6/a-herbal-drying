# 协作分工与交付位置

本仓库仅保存可复现代码、输入数据、结果文件与技术说明；**不提交**教师审阅稿、论文模板、历史草稿或缓存文件。

## 编程手

1. 拉取最新代码后依次运行：
   ```bash
   python scripts/build_deliverables.py
   python scripts/run_q3_audits.py
   python scripts/run_q4_audit.py
   ```
2. 将重新生成的 `result1.xlsx`—`result4.xlsx` 和 `outputs/**` 二进制结果以正常 Git 提交同步；核对表名、列名、单位与论文一致。
3. 复查问题四固定参考坐标 `xi=r/R(t)` 的守恒输出；当前相对残差上限应不大于 (5\times10^{-4})。
4. 若论文保留“端面近似造成 0.105 K / 0.39% 偏差”的定量结论，补充对应二维核验脚本和原始输出。

## 建模手

1. 审核问题四“等比例收缩、固定参考坐标”的假设与推导，确保不误写为干基质量坐标。
2. 统一端面表述：问题一至三采用中截面一维近似，端面效应经量级分析后省略；问题四按 `docs/model/问题4端面近似说明.md` 处理。
3. 审核附录 4 物性公式、单位与“忽略蒸发潜热”的适用边界。

## 论文整理

1. 用 `result1.xlsx`—`result4.xlsx` 回填所有结果表，并统一“干基含水率”与“烘房水分浓度”的用词。
2. 全文温度口径：(50.00\ ^\circ\mathrm C=323.15\ \mathrm K)；仅 Arrhenius 指数中的温度使用 K。
3. 更新附录 A 的实际脚本清单，删除失效脚本名与过时的“Q1–Q3 回归”表述。
4. 最终导出前，核对图表编号、交叉引用、页数、摘要四段及文件命名。

## 当前核验基准

- 问题二质量守恒残差上限：0.2471%；
- 问题三质量守恒残差上限：0.1094%；
- 问题四固定参考域质量守恒残差上限：0.0204%；
- 问题三阈值时刻：57.5471 h；问题四阈值时刻：51.1197 h。

